# ============================================================
# MatDiscoverAI - Vercel-optimized dependencies
# Lighter than requirements.txt to fit Vercel's 250MB limit
# Heavy ML libs (torch, transformers, chromadb, sentence-transformers)
# are excluded; the app falls back to regex NER + stub responses.
# For full features, use Render or Docker with the full requirements.txt.
# ============================================================

# ---------- Django Core ----------
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-environ==0.11.2
django-filter==24.2
django-extensions==3.2.3
dj-database-url==2.1.0
whitenoise==6.6.0
gunicorn==22.0.0
daphne==4.1.2

# ---------- Auth & Security ----------
djangorestframework-simplejwt==5.3.1
django-allauth==0.61.1
PyJWT==2.8.0

# ---------- Celery (EAGER mode on Vercel) ----------
celery==5.4.0
redis==5.0.4
django-celery-results==2.5.1

# ---------- ML / Data Science (light) ----------
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
scipy==1.13.0

# ---------- Optional ML (commented out to fit Vercel 250MB limit) ----------
# xgboost==2.0.3
# lightgbm==4.3.0
# optuna==3.6.1
# shap==0.45.1
# matplotlib==3.9.0
# plotly==5.22.0
# reportlab==4.2.0

# ---------- NLP (light) ----------
pdfplumber==0.11.0
pypdf==4.2.0
nltk==3.8.1

# ---------- LLM (HTTP only, no SDK heavy deps) ----------
openai==1.33.0
tiktoken==0.7.0
requests==2.32.3
httpx==0.27.0

# ---------- Knowledge Graph ----------
networkx==3.3
pyvis==0.3.2

# ---------- Database (Postgres for production Vercel) ----------
psycopg2-binary==2.9.9

# ---------- Utilities ----------
python-dateutil==2.9.0
PyYAML==6.0.1
tqdm==4.66.4
loguru==0.7.2
pydantic==2.7.3
Pillow==10.3.0
python-dotenv==1.0.1
